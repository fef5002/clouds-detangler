# Configuration for output paths
# Copy this file to config/paths.yaml and customize for your setup

# Where to save JSON manifests from rclone lsjson
manifests_dir: "./manifests"

# Where to save logs (optional)
logs_dir: "./logs"

# Where to save DuckDB database (optional, for future use)
database_path: "./clouds.duckdb"
